import React, { Fragment } from 'react';

import HomeHeader from './HomeHeader';

const HomePage = () => {
  return (
    <Fragment>
      <HomeHeader />
    </Fragment>
  );
};

export default HomePage;
