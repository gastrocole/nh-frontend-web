import React, { Fragment } from 'react';

import HomeHeader from '../components/home/HomeHeader';

const Home = () => {
  return (
    <Fragment>
      <HomeHeader />
    </Fragment>
  );
};

export default Home;
